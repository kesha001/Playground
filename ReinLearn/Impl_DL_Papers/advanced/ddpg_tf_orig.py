# Need a replay buffer class
# Need a class for a target Q network (funcion of s, a)
# we will use batch norm
# the policy is deterministic, how to handle explore exploit?
import sys
print(sys.version_info)